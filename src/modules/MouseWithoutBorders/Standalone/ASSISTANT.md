# AI-assisted development

This standalone Mouse Without Borders fork is being developed by `aeae1` with coding assistance from:

**aeae1's Vibe Coding Assistant — ChatGPT (GPT-5.6 Sol)**

GitHub writes made through the connected account are authenticated as `aeae1`, so the GitHub author/committer shown on those commits may be `aeae1`. To make AI-assisted changes easy to identify, commits produced through this workflow should include this trailer:

`Assisted-by: ChatGPT (GPT-5.6 Sol)`

This note is informational only; it does not grant the assistant an independent GitHub identity or credentials.
